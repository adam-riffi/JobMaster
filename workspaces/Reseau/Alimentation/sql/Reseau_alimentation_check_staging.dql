-- Vérifie si la staging staging_sites_reseau contient des lignes aujourd'hui
SELECT CASE WHEN COUNT(*) > 0 THEN 'OUI' ELSE 'NON' END
FROM £TD_TMP.staging_sites_reseau
WHERE CAST(date_maj AS DATE) = CURRENT_DATE;

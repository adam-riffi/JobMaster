-- Vérification que le SOCLE paie_employes a été mis à jour aujourd'hui
SELECT CASE WHEN COUNT(*) > 0 THEN 'OUI' ELSE 'NON' END
FROM £TD_SOCLE.paie_employes
WHERE CAST(date_maj AS DATE) = CURRENT_DATE;

-- Suppression des agrégats du mois courant avant recalcul (BigQuery)
DELETE FROM £BQ_HISTO.interactions_aggr_nps_mensuel
WHERE FORMAT_DATE('%Y%m', date_agregat) = FORMAT_DATE('%Y%m', CURRENT_DATE());

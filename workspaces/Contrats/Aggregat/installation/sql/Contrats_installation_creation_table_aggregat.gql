-- Création de la table agrégat contrats_aggr_actifs_mensuel (BigQuery)
CREATE TABLE IF NOT EXISTS £BQ_HISTO.contrats_aggr_actifs_mensuel (
  periode                  STRING         NOT NULL,
  nb_enregistrements       INT64,
  cumul_12m                INT64,
  date_calcul              TIMESTAMP
);

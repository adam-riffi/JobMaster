-- Vérification que le SOCLE interconnexion a été mis à jour aujourd'hui
SELECT CASE WHEN COUNT(*) > 0 THEN 'OUI' ELSE 'NON' END
FROM £TD_SOCLE.interconnexion
WHERE CAST(date_maj AS DATE) = CURRENT_DATE;

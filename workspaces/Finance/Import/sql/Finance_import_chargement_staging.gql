-- Chargement des fichiers finance dans la table staging BigQuery
LOAD DATA OVERWRITE £BQ_TMP.staging_finance
FROM FILES (
  format = 'CSV',
  uris = ['gs://telecom-landing/finance/*.csv'],
  skip_leading_rows = 1,
  field_delimiter = ';'
);

-- Vérifie si la staging staging_equipements contient des lignes aujourd'hui
SELECT CASE WHEN COUNT(*) > 0 THEN 'OUI' ELSE 'NON' END
FROM £TD_TMP.staging_equipements
WHERE CAST(date_maj AS DATE) = CURRENT_DATE;
